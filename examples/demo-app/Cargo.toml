[package]
name = "demo-app"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { version = "0.6", features = ["web"] }

# Note: No dependency on dioxus-iconify needed!
# Icons are generated as source code in your project.
